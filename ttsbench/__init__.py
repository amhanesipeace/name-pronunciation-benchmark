"""ttsbench — a small, extensible toolkit for the name-pronunciation
fairness benchmark.

Modules:
    dataset   load the list of names to test
    engines   TTS systems behind one common interface
    synth     run names through an engine and record a reproducible manifest
"""
__version__ = "0.1.0"
